#pragma once

#include <iostream>

/**
 * @brief Panic function that throws an exception
 * @param message The message to be displayed
 */
void PANIC(const char *message);

void log(const char *message);
